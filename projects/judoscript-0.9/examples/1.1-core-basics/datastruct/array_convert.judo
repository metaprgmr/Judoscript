usage {
  author  = 'James Huang';
  created = '2001-11-12';
  lastMod = '2002-08-10';
  desc    = 'Applies a lambda to convert an array (to all upper case).';
}

a = [ 'abc', 'Abc', 'ABc', 'ABC' ];
println a.convert( lambda elem { return elem.toUpper(); } );
